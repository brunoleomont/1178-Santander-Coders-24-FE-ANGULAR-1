import { GramasTransformPipe } from './gramas-transform.pipe';

describe('GramasTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new GramasTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
